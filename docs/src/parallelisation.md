```@meta
CurrentModule = Daedalus
```

This page is work in progress on documenting how to run `daedalus` as an [`EnsembleProblem`](https://docs.sciml.ai/DiffEqDocs/stable/features/ensemble/) with multi-threading.
In brief, this is possible, but requires manually starting Julia or running a Julia script with more than one thread allocated.
